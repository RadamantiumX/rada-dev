---
import Send from "./icons/Send.astro";
import ContactForm from "../reactComponents/ContactForm.jsx"
interface Props{
name: string;
message: string;
send: string;
}

const {  name, message, send } = Astro.props
---
<ContactForm
 name={name}
 message={message}
 send={send}

 client:load
/>