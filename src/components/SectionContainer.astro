---
const { class: className, id } = Astro.props
// Le pasamos mas estilos a traves de los *props*
---
<!--Componente para separar las secciones-->
<section id={id} class={`w-full mx-auto lg:w-[740px] ${className}`}>
   <slot/>
</section>